import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      light: "#1de9b6",
      main: "#6effe8",
      dark: "#00b686",
    },
    secondary: {
      main: "#00e5ff"
    }
  },
});

export default theme;
